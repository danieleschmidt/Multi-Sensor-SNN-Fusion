apiVersion: v1
kind: Service
metadata:
  name: snn-fusion-service
  namespace: snn-fusion
  labels:
    app: snn-fusion-api
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: snn-fusion-api
